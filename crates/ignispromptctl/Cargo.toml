[package]
name = "ignispromptctl"
version = "0.1.0"
edition = "2021"
description = "CLI for inspecting a running ignispromptd daemon"

[[bin]]
name = "ignispromptctl"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
flate2 = "1"
tar = "0.4"
ureq = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
